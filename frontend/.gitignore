# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js
# The Docker build uses `npm install --legacy-peer-deps` by design (it skips
# CPU/OS-mismatched optional binaries), so a lockfile is intentionally not
# committed — see frontend/Dockerfile.
package-lock.json
yarn.lock

# testing
/coverage

# production
/build

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
