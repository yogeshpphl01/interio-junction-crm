// PLACEHOLDER — regenerate with `flutterfire configure`, which overwrites this
// file with your project's real values (and generates the native config).
//
// Firebase *client* config is not secret: apiKey/appId/projectId are public app
// identifiers, safe to commit. The secret is the backend service account
// (FCM_CREDENTIALS), which never ships in the app.
import 'package:firebase_core/firebase_core.dart';

class DefaultFirebaseOptions {
  static FirebaseOptions get currentPlatform => const FirebaseOptions(
        apiKey: 'REPLACE_ME',
        appId: 'REPLACE_ME',
        messagingSenderId: 'REPLACE_ME',
        projectId: 'REPLACE_ME',
      );
}
