# Flutter / Dart
.dart_tool/
.packages
.flutter-plugins
.flutter-plugins-dependencies
pubspec.lock
build/

# Generated platform folders (run `flutter create .` locally to regenerate)
android/
ios/
linux/
macos/
windows/
web/

# Firebase native config is generated per-project by flutterfire configure.
# (firebase_options.dart IS committed — placeholder values, public client ids only.)
**/google-services.json
**/GoogleService-Info.plist

# IDE
.idea/
*.iml
.vscode/
